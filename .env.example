# Application
APP_ENV=dev

# Database driver (only "mysql" supported currently)
DATABASE=mysql
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=jarvishstack

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_smtp_password
SMTP_FROM=your_email@example.com
SMTP_SECURE=tls
